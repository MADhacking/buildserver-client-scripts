# Email address of the build-space administrator, if not defined any emails
# will be sent to root (or their local alias).
# ADMIN_EMAIL="system.admin@example.com"

# Base path for logs
LOGPATH="/var/log/autoupdate"

# The default update command parameters.
# UPDATE_OPTS="--update --deep --newuse --with-bdeps=y @world"
# UPDATE_FLAGS=""

# List of packages which should also be updated if there is 
# an update to portage available.
PORTAGE_DEPS="sys-libs/glibc app-shells/bash"

# List of packages which should not be automatically cleaned.
KEEP_PACKAGES="dev-lang/python"
